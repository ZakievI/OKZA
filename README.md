# Расчет обтекания крылового профиля при помощи обратных кравевых задач

#include "fcalc.h" делать в cpp вроде надо
после сделанного юнита в файле CMakeLists.txt добавить надо инклуды и сорсы. Пример есть.